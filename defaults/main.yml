---
python_packages: []
python_build_dependencies: []
python_requirements_file: omit
python_legacy_support: no
python_easy_install_executable: "{{ '/usr/local/bin/easy_install' if python_legacy_support else '/usr/bin/easy_install3' }}"
python_pip_executable: "{{ '/usr/bin/pip2' if python_legacy_support else '/usr/local/bin/pip3' }}"
